*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
npm-debug.log
yarn-error.log
*.sql

# Database backups
/backups/

# Student PII (NIM/nama/prodi) — never commit; upload to the server out-of-band
mahasiswa_feb.csv

# Docker bind-mounts for user uploads (opsi C: dalam repo, bukan di image).
# Isi file user tidak di-commit; folder dipertahankan via .gitkeep.
/data/uploads/*
/data/private/*
!/data/uploads/.gitkeep
!/data/private/.gitkeep
